import type { Options, Capabilities } from '@wdio/types';
import type { EdgedriverServiceOptions } from './types';
export default class EdgedriverLauncher {
    private options;
    private process?;
    constructor(options: EdgedriverServiceOptions);
    beforeSession(config: Options.Testrunner, capabilities: Capabilities.Capabilities | Capabilities.MultiRemoteCapabilities, _: never, cid: string): Promise<void> | undefined;
    afterSession(): void;
    _startDriver(config: Options.Testrunner, capabilities: Capabilities.Capabilities | Capabilities.MultiRemoteCapabilities, cid: string): Promise<void>;
    _stopDriver(): void;
    _mapCapabilities(capabilities: Capabilities.Capabilities | Capabilities.MultiRemoteCapabilities, path: string, port: number): void;
}
//# sourceMappingURL=service.d.ts.map